
const UpdatePromotion = ( imageInfo ) => {
//   const src = require(imageInfo.src);
  return (
    <>
      <img className="avatar border-gray-100" alt={imageInfo.alt} src={imageInfo.src} />
    </>
  );
};

export default UpdatePromotion;
